<Window x:Class="DnD_calc.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DnD_calc"
        mc:Ignorable="d" WindowStartupLocation="CenterScreen"
        Title="MainWindow" Height="650" Width="700">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="70"></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition Width="200"></ColumnDefinition>
            <ColumnDefinition Width="200"></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <StackPanel Grid.Row="0" Grid.Column="1" Grid.ColumnSpan="2"
                    VerticalAlignment="Center" HorizontalAlignment="Center">
        <Label Content="ОЧКИ" ></Label>
        <TextBox Text="0" Width="40" Height="25" IsEnabled="False" x:Name="PointScore"
                 HorizontalContentAlignment="Center" VerticalContentAlignment="Center"
            ></TextBox>
            
        </StackPanel>
        <StackPanel Grid.Row="1" Grid.Column="1"
                    VerticalAlignment="Top" HorizontalAlignment="Center">
            <TextBlock Text="ПОЛОЖИТЕЛЬНЫЕ" Foreground="Green"></TextBlock>
            <ListView x:Name="PositiveListView"
                HorizontalAlignment="Center" VerticalAlignment="Center">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <CheckBox x:Name="Positive_Check" Checked="Positive_Check_Checked"
                            Content="{Binding Char_Pos, StringFormat={}{0}}"   Unchecked="Positive_Check_Unchecked"
                                 VerticalAlignment="Center" HorizontalAlignment="Center"></CheckBox>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackPanel>


        <StackPanel Grid.Row="1" Grid.Column="2"
                    VerticalAlignment="Top" HorizontalAlignment="Center">
            <TextBlock Text="ОТРИЦАТЕЛЬНЫЕ" Foreground="crimson"></TextBlock>
            <ListView x:Name="NiggativeListView"
    HorizontalAlignment="Center" VerticalAlignment="Center">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <CheckBox x:Name="Niggative_Check" Checked="Niggative_Check_Checked"
                            Content="{Binding Char_Nigg, StringFormat={}{0}}" Unchecked="Niggative_Check_Unchecked"
                 VerticalAlignment="Center" HorizontalAlignment="Center"></CheckBox>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackPanel>
        <StackPanel Grid.Row="1" Grid.Column="3" VerticalAlignment="Top" HorizontalAlignment="Left">
            
        <TextBlock x:Name="GoodSkillList" Text="Положительные" Foreground="Green"
                 Height="Auto"></TextBlock>
        <TextBlock x:Name="BadSkillList" Text="Отрицательные" Foreground="Crimson"
                 Height="Auto"></TextBlock>
            
        </StackPanel>

    </Grid>
</Window>
